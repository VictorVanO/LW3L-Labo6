<html>
    <head>
        <title>Title</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <h1>IT Academy</h1>
        <h2>Panier</h2>
        <form action="/cart" method ="POST">
            <input class="cart-input" type="submit" value="Finaliser l'inscription" >
            <input class="cart-input" type="submit" value="Retourner au catalogue de formation" formaction="/">
        </form>
        <table>
            <tbody>
                <tr>
                    <td>Id</td>
                    <td>Nom</td>
                    <td>Prix</td>
                    <td>Début</td>
                    <td>Fin</td>
                    <td></td>
                </tr>
                <% for (let i = 0; i < cart.length; i++) { %>
                <tr>
                    <td><%= cart[i].idcart %></td>
                    <td><%= cart[i].name %></td>
                    <td><%= cart[i].price %></td>
                    <td><%= cart[i].start %></td>
                    <td><%= cart[i].end %></td>
                    <td><a href="/remove/<%= cart[i].idcart %>">Supprimer</a></td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </body>
</html>